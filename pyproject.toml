[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "gshock_time_server"  # Change to your project name
version = "1.0.0"
description = "Pythgon script to updqte time on GShock Watches using BLE"
readme = "README.rst"
authors = [{ name = "Ivo Zivkov", email = "izivkov@gmail.com" }]
license = { file = "LICENSE" }
dependencies = ["requests"]  # Add any required dependencies
